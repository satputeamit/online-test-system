apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  labels:
    name: gateway-service
    app: ots-app
spec:
  type: NodePort
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 30004
  selector:
    name: gateway-app-pod
    app: ots-app
        