apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  labels:
    name: dashboard-service
    app: ots-app
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30007
  selector:
    name: dashboard-app-pod
    app: ots-app
        